# Deprecated — see DATABASE_SETUP.md

The project no longer uses Supabase. The database is now **Neon Postgres**,
added through Vercel's Storage tab. Follow **DATABASE_SETUP.md** instead.
This file is kept only so old links don't break. Safe to delete.
